====================================================================================================
KEY: floating-feet-absolute-y  confirmed=True  conf=certain  sev=major
--- explanation:
placeAddressMarkers (ARPlacementView.swift:3765-3865) parents both foot markers to AnchorEntity(world: .zero). Since that anchor has an identity world transform, child positions are world coordinates. Lines 3840/3846 first set the feet to stanceCenter +/- footOffset — which carries the correct raycast floor y (ball.y = -0.8532 this session) — and then lines 3841/3847 clobber it with position.y = 0.08, an ABSOLUTE world height. ARKit's world origin is the phone pose at session start, and James starts sessions holding the phone at roughly waist height, so the floor always sits ~0.8-1.5 m BELOW world y=0. The markers therefore render at 0.08 - (-0.853) = 0.933 m above the floor this session — about knee/waist height — and a comparable height in every room, because the float distance equals 0.08 plus however high the phone was at session start. The telemetry masks the bug: the log payload (lines 3859-3861) reports stanceCenter (the intended floor position, stance_y = -0.8532), not the rendered y = 0.08. Git history shows the constant was always absolute (B55 0.01 -> B57 0.05 -> B62 0.08); each bump was justified as floor-relative LiDAR-mesh clearance, but RealityKit occlusion is never enabled (no sceneUnderstanding.options = [.occlusion] anywhere), so that whole rationale was moot — the actual pre-B77 invisibility was frustum culling of markers placed 60 cm behind the camera (B77 comment, lines 3814-3825). The 0.08 value only ever 'looked right' if a session began with the phone ~8 cm above the floor. Note H1 is purely visual: it explains the floating feet, not the rightward misses (those are other hypotheses). Separately observed: the feet straddling the ball-target line is the B77 DESIGN (feet at ball +/- perp*halfSpread puts one foot on each side of the line) — a distinct issue from the floating, and StanceGeometry.handToBallMetres exists but is unused for offsetting the stance toward the golfer's side. Also dropRollTrailMarker (line 3907) has the same absolute-y hardcode (0.0008), so roll-trail dots float ~0.85 m up too.
--- symptom_match:
Quantitatively exact for the floating-feet symptom: floor/ball at world y = -0.8532 (telemetry), markers hardcoded to world y = +0.08, so they render 0.9332 m above the floor — knee-to-waist height, matching James's 'foot markers float in the air' in every room (float height = 0.08 + phone-height-at-session-start, always ~0.9-1.6 m). Telemetry stance_x/y/z == ball position exactly because the log records stanceCenter before the y-overwrite, which is also why logs alone never caught it. This hypothesis accounts for 100% of the floating-feet symptom and 0% of the putts-miss-right symptom (it never touches physics, yaw, or the roll mapping). The straddling-the-line complaint is also NOT explained by H1 — that is the intentional B77 geometry (ball +/- perp*0.2266 m places one foot on each side of the ball-target line instead of both feet on the golfer's side).
--- fix_sketch:
In placeAddressMarkers (ARPlacementView.swift): replace the absolute overwrite with a floor-relative lift — leftFoot.position.y = ball.y + lift and rightFoot.position.y = ball.y + lift (ball.y IS the raycast floor height at the ball; this matches the B67 aim-line precedent 'to.y + 0.002' at line 3664). Cleaner variant: anchor at AnchorEntity(world: SIMD3(ball.x, ball.y + lift, ball.z)) and give the feet purely relative +/- footOffset child positions, matching the placeBall/placeHole pattern. Lift value: 0.002 m (2 mm), same as the B67 aim line — because RealityKit scene-understanding occlusion is never enabled (no arView.environment.sceneUnderstanding.options.insert(.occlusion) anywhere in the repo), the LiDAR-mesh-occlusion justification for 5-8 cm is inoperative and a 2-5 mm z-fighting lift is sufficient; 8 cm would visibly hover. If occlusion is ever turned on later, keep the lift small and exempt the markers instead (e.g. render them in a non-occluded layer or accept partial mesh occlusion), since a big lift trades a worse artifact (hovering decals) for a marginal one. While there: (1) apply the same fix to dropRollTrailMarker line 3907 — marker.position.y should be derived from the floor (e.g. ballWorldPosition.y or the roll-plane y) + 0.0008, not absolute 0.0008; (2) update the stale B57/B62 comments; (3) add the rendered marker y to the materialApplied log payload so telemetry catches this class of bug in future. Expected effect: feet render flat on the carpet at the ball, in every room, regardless of where the phone was at session start. Separate follow-up (not H1):
  >> REFUTATION [refute-math]: upheld
     corrections: One minor historical inaccuracy: the value lineage did not start at B55. B46 (commit 985110e) introduced the foot markers already on AnchorEntity(world: .zero) with position.y = 0.001 (matching the stale 'lifted 1 mm only' doc comment still at lines 3754-3756); B55 (9020e11) raised 0.001→0.01, then B57 0.01→0.05, B62 0.05→0.08. The finding's chain 'B55 y=0.01 → B57 0.05 → B62 0.08' reads as though B55 originated the constant. This strengthens rather than weakens the conclusion — the hardcode was world-absolute from day one (B46), and even the original 1 mm 'lift' floated at whatever height the phone started the session. Also trivially: the finding cites 'B62.4' but the commit is labeled B62 (0.6.7/62). Nothing else was wrong or overstated.
     risk: dropRollTrailMarker fix must NOT reuse the emitted world.y: BallRollAnimator.worldPos (BallRollAnimator.swift:314-322) returns ballStart.y + ballYLift (ball-CENTRE height, ~2.1 cm above floor), so deriving the marker y from the emitted position would
     risk: A 2 mm lift makes the feet coplanar-ish with the B67 aim line (to.y + 0.002) and the hole contact shadow (+0.0008); geometry says they don't overlap today (foot inner edge ~17.7 cm from the aim line), but give each decal family a slightly distinct li
     risk: ball.y is the raycast floor height AT THE BALL; the feet sit ~23 cm laterally — on a slightly tilted detected plane or uneven carpet a 2 mm lift can visually clip into the camera-feed floor at the marker far edge; 3-5 mm is a safer default than exact
     risk: If a future build enables RealityKit scene-understanding occlusion (.occlusion), 2-5 mm decals will be eaten by LiDAR mesh noise (±1-3 cm) and RealityKit has no per-entity occlusion exemption — the lift decision must be revisited then (the finding ac
     risk: The 'cleaner variant' (anchor at ball + lift, purely relative children) changes the anchor world transform per placement; clearAddressMarkers() is already called on every re-place (line 3781) so no stale-anchor accumulation, but keep that ordering if
  >> REFUTATION [fix-regression]: upheld
     corrections: Two things the finding's fix sketch under-specifies, one nit. (1) The dropRollTrailMarker sub-fix has a hidden trap the finding missed: BallRollAnimator has TWO emitter call paths — the roll loop passes WORLD coords via worldPos(from:) (lines 183/224), but dropBallIntoCup line 270 passes ballEntity.position, which is anchor-LOCAL (a pre-existing coordinate-frame bug: during the cup-drop animation the emitter already receives local coords, so trail dots during the drop get wrong x/z today). Also worldPos() returns y = ballStart.y + ballYLift (ball-radius ≈2.1 cm, lines 314-321), so the naive 'marker.position.y = world.y + 0.0008' would float trail dots a ball-radius up. The fix must derive y from scene.ballWorldPosition.y (the sketch's primary suggestion — correct) and either convert line 270 to world coords or skip emission during the drop phase. (2) The residual-visibility question is slightly understated: frustum culling explains pre-B77 stance-pose invisibility, but B56's 'markers invisible' report plausibly included placement-time viewing (user standing, markers 60 cm in FRONT to
     risk: Trail-marker sub-fix: dropBallIntoCup (BallRollAnimator.swift:270) passes anchor-LOCAL coords to trailEmitter while the roll loop passes world coords — deriving trail y from the callback's world.y inherits this mismatch and worldPos() y already inclu
     risk: 2 mm lift carries a small visibility-regression risk: B56's 'markers invisible at 1 cm' report predates the stance-pose frustum-culling explanation and may have included placement-time viewing; mitigate by shipping the rendered-y telemetry in the sam
     risk: Fixing the float makes the B77 straddle geometry (one foot each side of the ball-target line) far more conspicuous — James will now clearly see 'wrong stance' instead of 'broken floating UI'; bundle the handedness-aware stance-side offset (UserProfil
     risk: If the cleaner anchor-at-ball variant is chosen: keep AnchorEntity(world:) with a synchronous addAnchor — do NOT switch to plane-anchored AnchorEntity (asynchronous re-targeting would move the feet) and do NOT copy placeBall's deferred-add pattern wi
     risk: Zero existing test coverage on marker placement (no unit/UI test references placeAddressMarkers/addressMarkersAnchor/dropRollTrailMarker) — the fix is unprotected against re-regression; consider extracting marker-y computation into a pure testable fu
====================================================================================================
KEY: stance-straddle-geometry  confirmed=True  conf=certain  sev=major
--- explanation:
CONFIRMED. The current model is not a golf stance at all — it is a 'ball between the feet' face-the-hole stance, drawn floating in mid-air. Four distinct geometric errors, all verified against the session telemetry: (1) WRONG SIDE MODEL — stanceCenter = ball exactly, feet at ball ± perp*0.2266, so one foot sits 22.7 cm LEFT of the ball-hole line and the other 22.7 cm RIGHT of it, both at the ball's along-line station. A real golfer has BOTH feet on ONE side (RH = left side viewed from behind the ball), toe line set back ~25-40 cm perpendicular from the line. (2) WRONG SPREAD AXIS — the shoulder-width spread is applied ACROSS the line (±perp) instead of ALONG it (lead foot toward the hole at +aim, trail foot at -aim). Even shifting the current pair sideways would still leave a hole-facing stance. (3) WRONG FOOT ORIENTATION — B79's footRot points each rectangle's long axis ALONG the aim direction (parallel to the line); real feet point AT the line (perpendicular, yaw ± 90°). (4) FLOATING — position.y = 0.08 is absolute world Y on a world-origin anchor; the session floor is at y = -0.853 (origin = phone pose at start, held ~chest height), so the markers hover 0.933 m above the carpet. The B57 comment intended 'raised 5 cm above the floor' but the code never references the floor/ball height. Additionally: handedness is stored but read by nothing geometric (the model is mirror-symmetric, so it cannot ever pick the correct side), and handToBallMetres — the one quantity that IS the correct perpendicular setback (~0.295 m) — is dead code with a doc comment pointing it down the wrong axis (-aim instead of perpendicular-to-line).
--- symptom_match:
Quantitatively explains 100% of the foot-marker symptoms: 'markers float in the air' = 0.08 - (-0.853) = 0.933 m hover, exact; 'feet fall either side of the line' = computed feet (-1.0196, 0.08, 0.3859) and (-0.5914, 0.08, 0.5341) straddle the line at ±0.2266 m with the ball centered between them, exact; logged stance_x/y/z == ball and aim_yaw_deg 160.90 both reproduce from the code path, confirming this code ran. However, this hypothesis explains 0% of the physics symptom (all 3 putts rolling visibly RIGHT while classified 'pull/goes left'): the face angle is press-relative (yaw-at-impact minus yaw-at-press), so square is defined by the user's press gesture, not by where the markers told him to stand. Wrong markers could at most nudge his body alignment, but a press-relative pipeline zeroes that out. The consistent -11 to -18 deg deltas with rightward real-world rolls point to a sign/mapping error elsewhere (psi0 = -faceAngleRaw / +y=LEFT green frame / perp mapping in BallRollAnimator), i.e. a sibling hypothesis. H2 is a real, fully confirmed defect — but a UX-guidance defect, not t
--- fix_sketch:
Rewrite the stance block of placeAddressMarkers (ARPlacementView.swift ~3812-3849) and thread handedness through. (1) Pass handedness in: change the signature to accept the UserProfile (or a side enum) — all 7 call sites already construct StanceGeometry from userProfile, so the profile is in hand. (2) Side selection: side = (handedness == .right) ? -1.0 : +1.0 applied to perp (perp = aim x up = right of line, so -perp = left = RH stance side; matches the video showing James's real feet left of the line). (3) Stance center: stanceLineCenter = ball + perp * side * setback, with setback = Float(stance.handToBallMetres) + ~0.05 (toe-line clearance) ≈ 0.30-0.35 m — this finally consumes the dead handToBallMetres; fix its doc comment to 'perpendicular to the target line'. (4) Spread ALONG the line: leadFoot = stanceLineCenter + aim * footHalfSpread (toward hole), trailFoot = stanceLineCenter - aim * footHalfSpread; optionally bias ball slightly forward of center by shifting stanceLineCenter back along -aim ~0.02-0.05 m. (5) Foot orientation: long axis points AT the line → footRot = quat(angle: yaw + side * .pi/2, axis +Y) (i.e. local +Z along -side*perp). (6) Height: feet.y = ball.y + 0.005..0.05 (floor-relative lift, keep the B57 LiDAR-occlusion margin), never absolute 0.08. (7) Side validation at press: handlePressBegan already computes phonePos; log sign(dot(phonePos - ball, perp)) vs configured handedness, and if they disagree show a hint / re-place markers — camera at PLACEMENT time is too ambiguous (user stands behind the ball aiming the crosshair, near the line extension),
  >> REFUTATION [refute-math]: upheld
     corrections: Only quote-precision nits, none load-bearing: (a) The B57 comment literally reads "Y raised to 5cm (was 1cm in B55)" — it does not contain the words "above floor"; the floor-relative intent comes from the surrounding LiDAR-occlusion context. The finding's paraphrase adds those words but its substantive point (comment intends floor-relative, code sets 8 cm absolute world Y, and 8 ≠ 5 anyway) is exactly right. (b) Two further stale comments the finding did not cite strengthen it: the placeAddressMarkers doc block (3744-3756) still claims "60 cm behind the ball" and "lifted 1 mm only", and line 3833 claims "foot points TOWARD the ball". (c) Minor side-note for the eventual fix: StanceGeometry's own comment "70° from vertical (USGA standard)" is itself dubious — putter lie is conventionally ~70° from HORIZONTAL — but cos(70°) happens to be the correct horizontal projection for a 70°-from-ground lie, so the 0.295 m value is right despite the mislabeled convention.
     risk: Side-selection sign flip: 'left of the line' is convention-dependent (viewed-from-behind-ball vs golfer's own left). perp = aim x up = right-of-line viewed from behind the ball; RH feet belong on the -perp side. Verify on-floor/video before shipping 
     risk: B77 regression: markers were moved TO the ball precisely because the old behind-the-ball position was culled from the view frustum in stance pose. Moving feet to the golfer's own side puts them at/under his toes — they may again fall outside the down
     risk: Floor-relative Y: feet.y = ball.y + 5 mm may re-trigger the B57 LiDAR-mesh occlusion that motivated the (mis-implemented) raise; test 5 mm vs 5 cm lift on James's iPhone before settling.
     risk: Signature change touches 7 call sites plus the default parameter (.compute(profile: .default)); missing one (especially the Settings-save path which uses `updated`, and the tap path which uses getUserProfile()) would desync handedness between paths.
     risk: Preserve the B79 guard ordering (compute aim BEFORE clearAddressMarkers) when rewriting the stance block, or the co-located-transient marker-loss bug returns.
  >> REFUTATION [fix-regression]: upheld
     corrections: Two overstatements, both minor. (1) Fix-sketch step 7 (press-time side validation via sign(dot(phonePos - ball, perp))) is weaker than claimed: at press the phone IS the putter head sitting on the target line directly behind the ball (handlePressBegan logs phone_to_ball_m as a proximity check), so the perpendicular component of phonePos - ball is centimetres and sign-noisy; the user's BODY is offset to the side but the phone is not. The validator needs a magnitude gate (e.g. only trust |dot| > ~0.10 m) or should use a pre-press camera trajectory instead — as specified it would emit false handedness-mismatch hints. (2) StanceGeometry.swift's lie-angle comment says "70° from vertical" while using cos(70°); the math corresponds to the standard ~70°-from-horizontal putter lie, so the 0.295 m value is right but the cited geometry is wrong — fix the comment when repurposing handToBallMetres, and treat 0.295 m as a starting point (real toe-line-to-ball distance for eyes-over-ball putting is nearer 0.35-0.45 m), pending the on-floor test the finding already proposes. Everything else checked 
     risk: Marker-visibility regression (the B56/B70/B77 saga repeats): moving feet ~0.30 m to the golfer's side at floor level means James's REAL feet stand on top of the markers at address, and a 5 mm lift may put them back under the LiDAR occlusion mesh that
     risk: SettingsView.swift:67 footer currently states 'v1 foot markers are symmetric and face-angle sign is right-hand-mirrored' — after threading handedness into marker placement this honesty disclosure becomes false for the stance half while staying true f
     risk: Wrong-side placement for left-handers becomes possible for the first time: today's symmetric layout is equally wrong for everyone; after the fix, a stale/reset UserProfile (default .right) plants both markers on the WRONG side for a lefty — strictly 
     risk: Log-schema semantic drift: stance_x/y/z (ARPlacementView.swift:3859-3861) currently equals the ball position and was used as a code-path fingerprint in this very debugging session. After the fix it would silently mean the offset stance-line centre. A
     risk: tools/code_review/gemini_visual_audit.py check #4 still prompts 'are the yellow stance markers visible behind the ball?' — stale since B77 and doubly wrong after this fix; visual audits will return false failures until the prompt says 'two markers on
====================================================================================================
KEY: roll-render-mirror-flip  confirmed=True  conf=high  sev=critical
--- explanation:
CONFIRMED — the 2D-sim -> AR-world mapping mirrors the roll across the target line, AND it is currently cancelling a second, independent sign error (H4), which is why the on-screen direction matched reality while the text labels were wrong. The math: in ARKit's right-handed +Y-up frame, left-of-aim (seen from above) for horizontal aim a=(ax,0,az) is up x a = (az, 0, -ax). BallRollAnimator.swift:119 computes perp = (-aim.z, 0, aim.x) — the exact negation, i.e. RIGHT of aim — then localPos() renders green-frame +y (documented 'left') along it. With today's session numbers: aim = normalize(hole-ball) = (0.3272, 0, -0.9450); true left = (-0.9450, 0, -0.3272); code perp = (+0.9450, 0, +0.3272) = right. Stroke S1 (raw = -17.90deg, peakVel 0.138, cal 14.18): v0 = 0.138*14.18*0.90*sqrt(0.95) = 1.717 m/s, roll = v0^2/(2*0.0611*9.81) = 2.458 m, psi0 = +17.90deg, green end = (2.339, +0.755) ('left' in green frame); the animator renders this at world (0.674, floor, -1.503) = 0.755 m to the RIGHT of the ball->hole line (0.338 m right when passing cup range). S2: 0.116 m right (stops short); S3: 0.603 m right (0.228 m right at cup range). All three render RIGHT — matching the Gemini video ground truth. Meanwhile the second error: FaceAngleComputer raw = yaw(impact)-yaw(press) on CoreMotion attitude, which is CCW-positive about world-up irrespective of the phone being inverted (a world-Z rotation premultiplies the attitude, so ZYX yaw delta equals the physical rotation; inversion lives in roll/pitch, and pitch~0 here since device-x is horizontal — gravity.x = -0.01..-0.005 — so no gimbal trouble). James's negative deltas (-17.9, -10.9, -12.3) are therefore physical CW rotations = face OPENING for a right-handed golfer = real-world miss RIGHT. The pipeline labels negative as 'closed/pull/left' (BallPhysics doc + MarioKartAssist), which is backwards; the sim then sends the ball green-LEFT (wrong side), and the renderer mirror flips it back to world-RIGHT (accidentally true). Net: screen correct by double error; labels ('Face closed — ball goes left') carry only the single error and are wrong vs both screen and reality. Repair-order analysis for the recorded strokes: (1) Fix rend
--- symptom_match:
Quantitatively accounts for the full label/screen contradiction. Predicted on-screen lateral misses: S1 +0.338 m right at cup range (17.9deg), S2 0.116 m right but 0.43 m short (10.9deg, undershoot), S3 +0.228 m right at cup range, ending 0.603 m right (12.3deg) — video shows all 3 visibly missing right by ~5-15deg, consistent in direction and magnitude with the sim's 10.9-17.9deg. The mirror is NOT why balls go right on screen — the screen is accidentally TRUE (his face really opens, calibration mean -7.83deg = ~8deg habitual open under the corrected semantics) — it is why every stroke is simultaneously labelled 'pull/left'. 100% of the 3/3 'bucket says left, ball goes right' mismatch is explained by the mirror+sign pair; the overshoot on S1/S3 (roll 2.46/2.83 m for a 1.05 m putt) is a separate speed-calibration issue outside H3.
--- fix_sketch:
Fix BOTH errors in one change-set (fixing either alone mirrors the screen against reality): (1) Sign semantic at the SOURCE — FaceAngleComputer.compute (FaceAngleComputer.swift:73): negate to raw = ImpactDetector.wrapAngle(yawAtPress - yawAtImpact), establishing golf convention positive = open = push = right. Every existing consumer (MarioKartAssist:87, StrokeResultPanel:165-171, PuttHistoryView, ReplayHistoryView, SensorDebugView, BallPhysics psi0 = -faceAngleRaw which is CORRECT under this convention and needs no change) already assumes that convention, so they all become correct with zero edits; update the now-wrong doc comment at BallPhysics.swift:15-19. Version the change: bump StrokeReplay faceAngleRawMeaning to 'v3_press_attitude_delta_golf_sign' (StrokeReplay.swift:234) and the face_angle_pipeline log tag (ARPlacementView.swift:2577); invalidate or sign-migrate stored CalibrationProfile.faceAngleBiasRad (today's -7.83 flips meaning); have PuttHistoryView/ReplayHistoryView sign-flip v2-tagged records on display. (2) Renderer perp at the single mapping boundary — change perp to true-left (aim.z, 0, -aim.x) in BallRollAnimator.swift:119 AND the identical expression in placeAddressMarkers (ARPlacementView.swift:3784), ideally extracted into one shared leftPerp(of:) helper so both sites (and the upcoming one-sided-stance fix, plus any future minimap) share one convention. Apply the geometric fix at the perp definition, NOT by negating y in localPos — perpUnit is the only place the green->world handedness is encoded, and the foot-marker code consumes the same vector. Expe
  >> REFUTATION [refute-math]: upheld
     corrections: Three points, none touching the core hypothesis. (1) Evidence item 6's parenthetical 'only the leftFoot/rightFoot variable names are currently swapped vs reality' is BACKWARDS. footOffset lies along perp (= world-right of aim), and leftFoot = stanceCenter - footOffset, which puts the entity named leftFoot physically on the LEFT of the ball-target line in the natural down-the-line view (and rightFoot on the right). The names are accidentally CORRECT today; they become swapped only if the proposed perp flip is applied to placeAddressMarkers without also swapping the -/+ offsets. (2) Open question 2's conjecture that the inverted semantic originated in a CW-positive compass-heading source is unsupported by the current tree: MotionSample.compassYaw (MotionSample.swift:60-66) is a misnomer - it is the same CCW-positive ZYX attitude-yaw formula, not CMDeviceMotion.heading. The origin question remains open but the in-tree v1 'compass' path already used the CCW convention. (3) Minor calibration of the symptom match: the Gemini video confirms only the RENDERED ball direction (there is no real
     risk: Test fixtures encode the current (inverted) sign convention and will fail or silently mask the fix: PuttingLabTests/Physics/FaceAngleComputerPressAttitudeTests.swift names a +8-deg CCW quaternion 'pushYaw' (line 172) and asserts raw = yaw(impact)-yaw
     risk: Flipping perp inside placeAddressMarkers (ARPlacementView.swift:3784) is visually invisible today (symmetric +/- use) but silently swaps which physical side the leftFoot/rightFoot entities land on - and the finding's own naming note is inverted - so 
     risk: CalibrationModel.applyBias (CalibrationModel.swift:58-60) is still live code even though B78 stopped calling it on the stroke path; the stored faceAngleBiasRad (-7.83 deg today) flips meaning under the negation. If bias subtraction is ever re-enabled
     risk: Stored v2 replays: PuttHistoryView (:113-116, 190-194, 223-225) and ReplayHistoryView re-bucket persisted faceAngleRaw on every load; without the meaning-tag-gated sign flip (faceAngleRawMeaning v2 vs v3) history will silently mix conventions and old
     risk: The entire two-sided fix direction rests on CMAttitude's quaternion being the device-to-reference rotation (CCW-positive yaw). All documentary evidence supports this, but it has not been confirmed on-device in this codebase - run the finding's 30-sec
  >> REFUTATION [fix-regression]: upheld
     corrections: Three overstatements/omissions, none fatal: (1) 'Every existing consumer already assumes that convention, so they all become correct with zero edits' is wrong for the practice (non-AR) flow — PracticeSessionView.swift:458-472 still subtracts a live cal baseline whose inputs (TestSessionState.calibrationFaceAnglesRad) are persisted to UserDefaults across relaunch; a pre-fix baseline mixed with post-fix raws roughly DOUBLES the displayed error (+17.8 deg for a square-intent stroke). The finding's migration list (CalibrationProfile.faceAngleBiasRad, replay tags) misses this second persisted store. 'B78 deleted bias subtraction' is true only for the AR flow. (2) The peakImpact log payload (ARPlacementView:2565-2580) computes press_to_impact_delta_yaw_deg inline, independent of FaceAngleComputer — after the source negation, face_angle_raw_deg = -press_to_impact_delta_yaw_deg within the same log line; offline analyzers (tools/bundle_analyzer.py) need this documented/renamed, and the finding's proposed on-device verification ('assert logged delta positive') works only because that key bypas
     risk: ATOMICITY: renderer perp flip and face-angle sign negation must land in one change-set — verified that either alone renders the ball on the wrong side of reality (renderer-only: screen mirrors against his real push-right; sign-only: labels true but s
     risk: FALSE-GREEN TESTS: FaceAngleComputerPressAttitudeTests tests a test-local computeWithPress extension, never production FaceAngleComputer.compute(); post-fix it passes while documenting the wrong convention ('positive yaw = PUSH', tests 2/3/9). Rewrit
     risk: FAILING TESTS NEEDING CONVENTION UPDATES: PressToImpactIntegrationTests lines 21/39/71-75/94/129/145 (incl. the staleBiasProfile would-have-been-corrected guard whose sign math must be re-derived) and ImpactDetectorTests:350-355 (pull(10) expects -10
     risk: MISSED MIGRATION SURFACE: TestSessionState.calibrationFaceAnglesRad persists to UserDefaults and its mean is LIVE-subtracted in PracticeSessionView's 'face (cal)' row — a pre-fix baseline (-7.8 deg) against post-fix raws displays ~+17.8 deg. Clear or
     risk: LOG SCHEMA SKEW: ARPlacementView:2565-2580 computes press_to_impact_delta_yaw_deg inline (impact-press); after negating compute(), face_angle_raw_deg = -delta in the same payload. Rename keys or bump the face_angle_pipeline tag exactly as sketched AN
====================================================================================================
KEY: face-angle-euler-inverted-grip  confirmed=False  conf=high  sev=major
--- explanation:
H4's two stated mechanisms are REFUTED by the quaternion math; a different, real sign defect was confirmed in the same chain. (1) Sign inversion from the upside-down grip: does not exist. CMAttitude's quaternion is device->reference (empirically confirmed from the telemetry: the alternative convention predicts press yaws of -59..-68 wobbling 10 deg with the stroke-to-stroke gravity-tilt change; logs show stable +73..+76). For a device->reference rotation, premultiplying by a world-up rotation adds EXACTLY to ZYX Euler yaw: a +10 deg CCW physical heading rotation of the inverted phone reads +10.0000 deg, sign-correct, at James's exact reconstructed press attitude. (2) Euler pitch/roll crosstalk: ZYX yaw equals the horizontal azimuth of the device x-axis, and for this grip the face axis IS +/-x, so yaw-delta is mathematically identical to 'project the face axis onto the horizontal plane at press and impact and take the signed angle about up' — the correct definition from item (5). Pure pendulum pitch (rotation about the horizontal axis perpendicular to the target line) moves Euler yaw by 0.0000 deg; with +/-20 deg pitch, +/-5 deg roll and up to 10 deg swing-axis misalignment the worst-case artifact is 2.5 deg (3.0 deg at pitch 30/roll 8/misalign 10). Crosstalk cannot produce -11..-18, nor the calibration mean -7.83. (3) What IS wrong: the measurement is CCW-positive about world up, so negative = face rotated clockwise = OPEN = ball starts RIGHT of the line — for any golfer (left/right of the line is geometry, not handedness). Downstream, BallPhysics line 239 (psi0 = -faceAngleRaw, '+y = LEFT', comment admits the flip is a right-handed-user HACK) and MarioKartAssist (negative = closed/pull/left) both assume the opposite, golf-style 'positive = open' convention. The render survives only because BallRollAnimator's perp vector is mislabelled (documented 'left', actually right in ARKit Y-up), cancelling the psi0 flip — so the on-screen ball faithfully rolls RIGHT for an open (-) reading while the coaching text says 'ball goes left'. That is exactly the observed text-vs-video contradiction. (4) Therefore the -10.9/-12.3/-17.9 deg readings are REAL clockwise (open) headi
--- symptom_match:
Quantitatively: the hypothesized mechanisms explain almost none of the numbers. Euler crosstalk accounts for at most ~2.5-3 deg worst-case (0-0.5 deg for a clean pendulum) of the observed -10.9..-17.9 deg, i.e. under 20% of the smallest reading; grip-induced sign inversion accounts for 0 (a +10 deg world-CCW rotation reads +10.0000 with the phone inverted). What the lane DID nail: the confirmed sign-convention mismatch explains 100% of the 'classified pull/ball-goes-left while video shows right misses' contradiction — measured negative physically means OPEN/right, the text maps it to closed/left, and the rendered ball goes right because the psi0 flip and the mislabelled perp vector cancel. The rendered miss magnitude equals |faceAngleRaw| = 10.9-17.9 deg, consistent with Gemini's 5-15 deg screen estimate. The magnitudes themselves (-11..-18 on intended-square strokes, cal mean -7.83) are real face-axis rotations at the detected-impact moment and are NOT explained by H4 — they point to impact-timing (H5: sample taken while the face is still open, i.e. early relative to the stroke bott
--- fix_sketch:
Three coordinated changes. (1) Keep the measured quantity but make it explicit and guarded: in FaceAngleComputer, compute the face angle as the signed horizontal-plane angle (about world up) between the device x-axis at press and at impact (worldX = R(q)*[1,0,0], zero the z component, atan2 of the cross/dot) — numerically identical to today's Euler-yaw delta but self-documenting, and add a guard for |elevation of x| > ~70 deg where the projection degenerates. Swing-twist about world up of q_impact*q_press^-1 agrees within ~0.6 deg; the projected-axis form is preferred because it is exact for the face axis and its conditioning is explicit. (2) Fix the sign convention ONCE, at the boundary, and fix BOTH cancelling errors together or neither: in BallPhysics set psi0 = +faceAngleRaw (CCW-positive = launch left, matching '+y = LEFT'), and in BallRollAnimator set perp = (aim.z, 0, -aim.x) (the true left vector, matching its doc comment). Fixing only one of the two will flip every rendered putt to the wrong side. (3) In MarioKartAssist, invert the bucket mapping: positive (CCW) = ball left = 'pull' for a right-handed golfer, negative = ball right = 'push' (and surface a handedness setting later — pull/push naming, not left/right geometry, is what depends on handedness). Expected effect: coaching text matches the rendered roll and physical reality ('Face open - ball goes right' on these strokes). This will NOT shrink the -8..-18 deg magnitudes — route that to the impact-timing hypothesis (H5): verify where the peak-velocity sample falls relative to the stroke bottom, and consider r
====================================================================================================
KEY: impact-timing-past-contact  confirmed=True  conf=high  sev=major
--- explanation:
CONFIRMED as a real, codebase-admitted contributing mechanism. The face angle is read at the instant of peak integrated hand velocity, and that instant systematically lands in the follow-through, past the moment the phone passes the address/ball position — so the detector reads extra face rotation that accrued AFTER 'contact'. Four mechanisms stack, the first being dominant and inherent to the definition itself: (1) DEFINITIONAL LATE BIAS — peak velocity coincides with ball passage only for an ideal free pendulum. Golfers are coached to accelerate THROUGH the ball, and in this product there is no physical ball collision to decelerate the 'putter', so the true hand-speed maximum occurs 100-300 ms beyond the address point, like a practice swing. The codebase itself corroborates: B12 data showed James judging 11/12 haptics 'felt late' with the comment admitting 'the algorithm impact already being slightly post-perceived-contact', and B13 data shows forward-swing peaks as late as 2317 ms after touchDown. (2) SIGNAL FRAGILITY — peak velocities today were 0.07-0.15 m/s (S2 at 0.069 is only 1.4x the 0.05 snap floor). Gravity leakage into userAcceleration during the 40-55 deg-tilted, rotating grip is ~0.17 m/s^2 per degree of attitude-filter error; even 0.5 deg of error integrates to ~0.04 m/s over the stroke — 30-60% of the whole peak. The linear detrend (v[end]=0) removes only the linear component; the residual tilts a broad, flat-topped velocity hump whose argmax shifts by Delta_t = s*tau^2/(2*v_p) ~ 20-60+ ms per modest residual slope, and the later-extremum tiebreak resolves ambiguity toward LATER peaks. (3) PCA over the whole window lets follow-through / pre-release phone movement reshape the projection axis. (4) The 5-pt (50 ms) moving average adds +/-10 ms jitter only — minor. THE MATH: detected press-to-impact yaw deltas were -17.9/-10.9/-12.3 deg inside 1.3-1.7 s windows. The forward stroke is ~0.3-0.6 s, implying a face sweep rate of roughly 30-60 deg/s through and past contact. Every 100 ms of late sampling therefore costs 3-6 deg of spurious 'closed' (or, physically, post-contact arc) rotation. If James's true face at ball passage is the 0-3 deg expected of
--- symptom_match:
Strong quantitative match on magnitude: detected deltas -10.9..-17.9 deg vs video misses of ~5-15 deg — a 150-400 ms late read at the telemetry-implied 30-60 deg/s face sweep produces 8-18 deg of spurious rotation, accounting for essentially all of the excess over a plausible 0-3 deg true at-ball face error. The -7.83 deg calibration bias on intended-square strokes is an independent measurement of the same systematic offset (~150-250 ms late at 35-50 deg/s) and B78 stopped subtracting it. The in-code B12 ('felt late' 11/12, algorithm 'post-perceived-contact') and B13 (forward peaks to 2317 ms post-touchDown) data confirm the lateness empirically. What H5 does NOT explain: the direction inversion (all three strokes labelled pull/LEFT while the ball visibly went RIGHT) — that requires the separate sign-mapping bug. So H5 accounts for most of the error MAGNITUDE and the consistency of the offset, not the sign.
--- fix_sketch:
Redefine impact as 'phone returns to the address position', using data already captured. In ImpactDetector.detect() (Physics/ImpactDetector.swift), when arkitPoses has adequate coverage with .normal tracking: (1) p0 = first pose position (press/address); (2) find max departure d_max during the window and require d_max > ~0.10 m (hysteresis so t=0 isn't picked); (3) after the max-departure time, find t* minimizing |p(t)-p0| (closest re-approach) with parabolic refinement over the 60 Hz pose track; (4) convert t* to the motion-sample timeline (both clocks are boot-time based) and slerp the IMU attitude at t* for FaceAngleComputer — keep the velocity peak only for speed/distance, and as the impact-time fallback when ARKit tracking is degraded (trackingState is already recorded per pose; ARPlacementView.swift lines 2355-2363). FaceAngleComputer.compute() already receives arkitPoses and impactTime and currently discards them, so the plumbing exists end-to-end (handlePressEnded already passes posesDuringRecording at line 2508). Cheap interim mitigations while building that: (a) log impact.timestamp - window.start per stroke (one line in the peakImpact payload) to measure the press-to-impact latency distribution directly; (b) constrain the peak search to end at the first post-peak-departure closest-approach estimate or cap it (e.g. disallow impact in the final 30% of the window); (c) alternatively reuse the LiveImpactDetector arm/fire structure but read attitude at the START of the rotation-rate descent rather than after confirmation. Expected effect: face angle sampled at geometr
  >> REFUTATION [refute-math]: partially-upheld
     corrections: (1) Evidence item 2 (LiveImpactDetector 55-60) is misused: 994-2317 ms is measured from touchDOWN and includes a variable address-hold plus the backswing (backswing peaks alone span 469-1023 ms post-touchDown). It says nothing about lateness relative to BALL PASSAGE; 'a 2.3 s peak is deep in follow-through' is an invalid inference — it may simply be a long hold or a slow stroke. That stat exists to tune a haptic fire-delay gate, not to measure contact-relative latency. (2) The 150-400 ms lateness figure is not measured anywhere; it is back-derived by assuming James's true at-ball face error is 0-3 deg AND that all residual is timing. The in-code admission says 'slightly post-perceived-contact', and B13's remedy was shaving 20-30 ms of confirmation latency — treated as perceptible and worthwhile, which is hard to reconcile with a 150-400 ms underlying offset (a haptic that late would be flagrant, not marginally 'felt late' on 11/12 strokes). 50-150 ms (~1.5-9 deg at the assumed sweep rates) fits the evidence at least as well, in which case timing explains a substantial fraction but NO
     risk: The 0.10 m max-departure hysteresis may exceed the real stroke amplitude on short putts: a 1 m putt needs only a cm-scale backswing, and today's integrated velocities imply cm-scale IMU displacement — many strokes could silently fail the d_max gate a
     risk: ARKit pose quality mid-swing is unverified (camera pointed at legs/floor, motion blur, featureless carpet): arView.session.currentFrame polled at 100 Hz duplicates ~60 Hz frames and can freeze/go stale during fast motion; closest-approach on a cm-sca
     risk: Clock alignment is real but not free: ARFrame.timestamp is the CAPTURE time of the last-rendered frame and lags 'now' by pipeline latency (~17-50 ms), and the pose track is effectively ~60 Hz — the 'zero new capture plumbing' claim holds for capture,
     risk: p0 = first recorded pose assumes the user does not adjust after pressing; a forward-press habit shifts the reference. Safer: closest approach to the BALL anchor's known world position (phone_to_ball_m is already logged at touchDown), with p0-vs-ball 
     risk: Shipping the timing fix BEFORE the left/right sign fix could collapse magnitudes under MarioKartAssist's 6-deg square threshold, masking the still-present sign defect as 'SQUARE' and making it latent/undiagnosable — fix the sign mapping first or toge
  >> REFUTATION [fix-regression]: partially-upheld
     corrections: The finding itself is accurate and self-aware about not explaining the left/right flip; corrections are to the fix sketch, not the diagnosis. (1) 'Zero new capture plumbing' is true for capture but glosses over persistence: StrokeReplay schema v2 carries no arkitPoses, so the offline-replay debugging workflow silently stops reproducing on-device results — a schema v3 is required, which IS new plumbing. (2) The pose track is described as '~100 Hz (one ARFrame camera pose per motion sample)' — technically what the code does, but ARFrames refresh at 60 Hz so ~40% of appended poses are duplicates; closest-approach refinement needs timestamp dedup or the parabolic fit divides by near-zero spans. (3) The d_max > 0.10 m hysteresis constant is presented as settled; the session's own telemetry (peak 0.069-0.148 m/s over a 0.3-0.6 s forward stroke) suggests soft short putts may never reach 10 cm of departure, making the new path silently no-op exactly where it is most needed — the threshold must be measured before being fixed. (4) Minor framing overstatement: the later-extremum tiebreak (useMa
     risk: 0.10 m max-departure gate likely exceeds real hand displacement on soft 1 m putts (telemetry peaks 0.069-0.148 m/s) — new path silently falls back to the late peak-velocity read on exactly the strokes that are most fragile today; tune from logged d_m
     risk: Global-minimum re-approach can pick a post-follow-through return to the address position (press is held through follow-through; lift-to-watch motion) — impact time LATER than today on some strokes; must be first-local-minimum-below-threshold
     risk: Replay reproducibility breaks: StrokeReplay v2 has no arkitPoses field, so offline replays of on-device strokes re-run the fallback path and produce different impactTime/faceAngleRaw than shipped results; StrokeReplayTests round-trip determinism stil
     risk: Zero existing test coverage of the new path: every test calls detect(in:) with arkitPoses=[]; Generator.swift fixtures have no camera-position model; once pose fixtures exist, ImpactDetectorTests impact-time-within-5ms and PressToImpactIntegrationTes
     risk: Practice mode engages the new path implicitly (PracticeSessionViewModel appends arkit.latestPose per sample and passes it to detect) with unverified pose quality in that grip — behavior becomes path-dependent per stroke; log which impact definition f
====================================================================================================
KEY: sweep-sign-consumers  confirmed=True  conf=high  sev=critical
--- explanation:
TWO independent sign errors live in the chain, and they cancel on screen while making every text label wrong. (1) Semantic inversion at the producer: FaceAngleComputer emits faceAngleRaw = yaw(impact) − yaw(press) with CoreMotion's positive=CCW-from-above. For a right-handed golfer (target to his left), closing the putter face rotates the face normal CCW viewed from above (square=toward hole at 180°, closed=left-of-line at ~225°; cross-product positive), so physically CLOSED = POSITIVE delta. The codebase convention used by every downstream consumer (BallPhysics doc §3.3, MarioKartAssist, PUTT_CALCULATION.md: 'negative = closed = pull left') is therefore inverted vs. reality: James's −11°..−18° deltas mean he left the face OPEN (push) — and the video duly shows right misses. (2) Renderer mirror: BallRollAnimator maps the green-frame lateral axis with perp = (−aim.z, 0, aim.x), which in ARKit's y-up right-handed world is RIGHT-of-aim (true left = (aim.z, 0, −aim.x)). BallPhysics computes psi0 = −faceAngleRaw, sending the negative-raw 'pull' to +y=LEFT in the green frame; the renderer then mirrors +y onto world RIGHT. Net math for S1: raw −17.9° → psi0 +17.9° → green-frame ball 17.9° left → rendered 17.9° RIGHT of the aim line. The double inversion makes the rendered ball direction accidentally match physical reality (open face → right), while MarioKartAssist labels the same stroke 'Pull — ball goes left'. Every other consumer is internally consistent with the documented convention: MarioKartAssist (negative→pull), StrokeResultPanel (duplicated 6/12/20 thresholds + negative→pull), PuttHistoryView (re-buckets stored raw), StrokeRecord/StrokeReplay (persist raw + bucket), while ReplayHistoryView/SensorDebugView (signed display) and StatsAggregator (abs only) are sign-agnostic. Bias: CalibrationModel.applyBias has zero production callers — CalibrationProfile.faceAngleBiasDeg (−7.83 today) is computed/saved/loaded but consumed ONLY by logging/manifest/diagnostic-mirror telemetry; the one live legacy bias-like path is PracticeSessionView's display-only 'face (cal)' row, which subtracts the session-local TestSessionState.calibrationFaceBaselineRad (UserDefaults-persiste
--- symptom_match:
Quantitative: S1 raw −17.9° → rendered 17.9° right of aim; S2 → 10.9° right; S3 → 12.3° right. Gemini's video read of 'all 3 missed RIGHT by ~5–15°' matches direction 3/3 and magnitude within visual-estimation error (11–18° computed). All 3 strokes bucketed pull/slightPull with 'ball goes left' — anti-correlated with the screen 3/3, exactly James's complaint that the text contradicts what he sees. The −7.83° calibration mean on intended-square strokes = a consistent ~8° open-face tendency under the corrected semantic, coherent with 'every putt goes right'. This sweep accounts for 100% of the direction/label symptom; the floating, line-straddling foot markers are confirmed as separate bugs (absolute y=0.08 on a world-zero anchor with floor at −0.853 → ~0.93 m hover; ±perp straddle by design error) and are corroborated but not caused by the sign chain.
--- fix_sketch:
Option C is the only complete repair; A or B alone each make the app WORSE (A: screen flips to agree with the labels but both then contradict reality — open-face stroke would render and read 'pull left'; B: labels become physically correct but the rendered ball flips to the wrong side, screen now contradicts both label and reality). Concrete C, exactly two load-bearing lines plus hygiene: (1) FaceAngleComputer.swift:73 — establish the golf convention at the producer: raw = ImpactDetector.wrapAngle(yawAtPress − yawAtImpact) for right-handed users (i.e. negate the delta so physically-closed/CCW becomes negative=pull-left as documented); make the negation handedness-conditional on UserProfile.handedness (already exists, Models/UserProfile.swift) — for left-handed users the un-negated delta is correct under the same 'negative=pull-left' convention. This also retires the BallPhysics psi0 'HACK assumes right-handed' comment: psi0=−faceAngleRaw becomes handedness-independent once the convention is enforced upstream. (2) BallRollAnimator.swift:119 — perp = SIMD3<Float>(aim.z, 0, −aim.x) so green +y renders truly LEFT of aim. Hygiene: bump StrokeReplay faceAngleRawMeaning to 'v3_press_attitude_delta_golf_sign' (StrokeReplay.swift:234) and tag the peakImpact log payload so offline tools (ingest_strokes.py, replay_viz.py, bundle_analyzer.py) can segregate semantics; update producer-level tests (FaceAngleComputerPressAttitudeTests, PressToImpactIntegrationTests) — BallPhysicsTests and MarioKartAssistTests stay untouched because those layers were internally correct; add one new end-to-e
  >> REFUTATION [refute-math]: upheld
     corrections: Two minor points, neither touching the core finding. (1) The left-handed half of the fix_sketch is muddled: it says 'for left-handed users the un-negated delta is correct under the same negative=pull-left convention'. My derivation agrees LH closed = CW = negative raw, and the 'pull/closed' LABEL would then be right un-negated — but for an LH golfer a pull goes RIGHT of the target line, not left, so BallPhysics/renderer (negative raw -> +y -> rendered left after the perp fix) would put the ball on the WRONG side for LH, and MarioKartAssist's copy 'ball goes left of the line' is RH-specific. A producer-only handedness negation cannot fix LH; the toggle must also condition the green-to-world lateral mapping (or psi0) and the label copy. The finding did flag LH as an open question, so this is an overstatement in the sketch, not an error in the finding. (2) 'yaw(Rz(theta)R)=yaw(R)+theta exactly' silently assumes the device x-axis is not vertical (ZYX gimbal degeneracy); immaterial here because the gravity telemetry (x approx -0.005..-0.012) proves device x stayed horizontal in James's gr
     risk: Options A (renderer-only) or B (producer-only) shipped alone each make the app actively worse — the two sign fixes must land atomically with an end-to-end test (physically-CCW quaternion sequence -> 'pull' label AND rendered offset on true-left).
     risk: Persisted-data semantics flip: StrokeRecord/StrokeReplay rows written pre-fix carry the old sign, and PuttHistoryView re-buckets stored raws under whatever semantics the running build assumes — old strokes will silently read as their mirror image unl
     risk: StrokeResultPanel.swift:165-180 duplicates MarioKartAssist's thresholds and pull/push wording — forgetting it desynchronises the result panel from the bucket/cause line.
     risk: The left-handed toggle as sketched (skip the producer negation for LH) is wrong: it fixes LH labels but mirrors the LH rendered ball, because pull=left is RH-only language; LH support needs a second conditional in the lateral mapping or label copy, v
     risk: Today's calibration profile (faceAngleBiasDeg=-7.83, speedToDistanceFactor=14.18) was learned under old semantics — recalibrating BEFORE the fix, or reintroducing bias subtraction without recalibration, bakes the wrong sign in; offline tools (bundle_
  >> REFUTATION [fix-regression]: upheld
     corrections: Four overstatements/gaps, none fatal: (1) 'bump faceAngleRawMeaning … so offline tools can segregate semantics' overstates — NONE of the offline tools read the tag today (verified: tools/ingest_strokes.py:154, tools/puttinglab/replay_viz.py:94, tools/simulate_historical_strokes.py:166 all read faceAngleRaw unconditionally), and simulate_historical_strokes.py is missing from the finding's tool list entirely. The tag bump is inert until those tools are taught to branch on it; that is additional required work, not automatic protection. (2) The fix sketch resolves only the label/semantics contradiction — all three of James's literal visual complaints (floating feet, straddling feet, ball missing right) remain visually identical after Option C. The finding's symptom_match says this for the feet but the fix_sketch contains no foot-marker repair; for a fix-review the marker fix (floor-relative Y instead of absolute 0.08, and one-sided stance using the unused StanceGeometry.handToBallMetres + handedness) must be named as a same-release companion. (3) PracticeSessionView:458-472 is framed as 
     risk: Partial landing is worse than the bug: producer negation without the renderer perp flip renders the ball LEFT while reality and the (now-correct) label say right; renderer flip alone makes the screen agree with the physically wrong 'pull left' label.
     risk: CI breakage unless updated in the same commit: FaceAngleComputerPressAttitudeTests:55 and :238 pin the old sign; PressToImpactIntegrationTests:73 hand-subtracts faceAngleBiasRad; FuzzTests/InvariantTests/SampleRateRobustnessTests/NoiseRobustnessTests
     risk: BallRollAnimator has zero existing test coverage and localPos/worldPos are private — the perp flip can land wrong-side with no CI signal. The proposed end-to-end test (closed-face quaternion sequence → 'pull' label AND true-left world offset) is load
     risk: Stale non-AR baseline corrupts display post-fix: PracticeSessionView:458-472 subtracts UserDefaults-persisted TestSessionState.calibrationFaceBaselineRad; an old-sign baseline against new-sign raws doubles the shown error. Invalidate/clear it in the 
     risk: Mixed-semantics history: PuttHistoryView:114-116 re-buckets stored pre-fix raws under post-fix assumptions and currently ignores faceAngleRawMeaning; old strokes will show flipped labels relative to identical new strokes unless the view filters/migra
====================================================================================================
KEY: sweep-calibration-coupling  confirmed=True  conf=high  sev=major
--- explanation:
What the 5 calibration strokes compute today: mean tempo, mean v2 press-attitude face angle (stored as faceAngleBiasRad = -0.1367 rad = -7.83 deg), and speedToDistanceFactor = sqrt(targetFt*19.7/stimp)/(meanPeakVel*3.281) with targetFt hardcoded to 10. (1) BIAS: still computed from v2 face angles and persisted, but NO production physics/classification path applies it — handleStrokeCompleted uses rawImpact (ARPlacementView:2564), touchUp uses rawResult (PracticeSessionViewModel:396), MarioKart buckets impact.faceAngleDegrees which is defined as raw (ImpactResult:38-40), and CalibrationModel.applyBias has zero callers outside tests. It survives only as logging/export dead weight — EXCEPT one genuinely latent application: PracticeSessionView:458-467 still subtracts the in-session cal baseline at display time ('face (cal)'), so practice mode and AR mode report different face angles for the same stroke. (2) SPEED: 14.18 is 'sane' only by accident. The calibration objective uses the dead DistanceModel formula (d = fps^2*S/19.7, no launch losses), but the live roll uses BallPhysics with v0 = peak*factor*0.90*sqrt(0.95) and mu = 0.611/S — which delivers only 38.5% of the objective distance. James's mean cal stroke (0.0954 m/s) therefore rolls 1.175 m (3.85 ft) in the sim, not the promised 10 ft; the objective-consistent factor would be ~22.8, not 14.18. Today this 2.6x mis-scale was masked because the hole was at 1.047 m (~3.4 ft), almost exactly the accidental effective calibration distance. For the 3 strokes (v0 = peak*12.44, decel 0.599 m/s^2): S1 0.138 -> rolls 2.46 m (2.35x hole, LONG); S2 0.069 -> 0.61 m (0.59x, SHORT); S3 0.148 -> 2.83 m (2.70x, LONG). S1/S3 would have arrived at the cup at 1.30/1.46 m/s — below the 1.626 capture velocity — so with a correct face angle two of three putts would have been DRAINED; the face-angle push-right turned them into long misses. The quadratic d~v^2 scaling means his 2.1x peak-velocity spread became a 4.6x distance spread. (3) The -7.83 deg bias is the smoking gun that the v2 'press = square' premise reads systematically non-zero on James's intended-square strokes (consistent with the H4/H5 yaw-definition problem given the in
--- symptom_match:
Calibration does NOT cause the miss-RIGHT direction — confirmed the bias (-7.83, which would push results LEFT if subtracted) is applied nowhere in the AR roll/bucket path, matching face_angle_deg == face_angle_raw_deg in telemetry. The -7.83 deg profile is rather independent EVIDENCE for the face-angle-definition sweeps: James's square-intent strokes systematically read negative (pull/left text) while balls visibly went right — a sign/definition error upstream of calibration. On distance: predicted long (2.35x), short (0.59x), long (2.70x) vs video 'all 3 rolled past'. Two of three quantitatively match 'rolled past'; S1/S3 long by 1.4-1.8 m. S2 (peak 0.069) is predicted to stop 0.43 m SHORT, contradicting the video claim — either the Gemini video read is imprecise for S2 or there is residual world-frame scaling not in this subsystem. Notably, if the calibration objective were internally consistent (factor ~22.8), all three strokes would roll past (1.5x-7x), matching the video better — circumstantial evidence the effective on-screen scale may be larger than the 14.18 chain predicts.
--- fix_sketch:
1) Unify the distance objective: in CalibrationModel.compute, derive the factor by inverting the LIVE sim chain — v0_needed = sqrt(2*(0.611/stimp)*9.81*targetMetres); factor = v0_needed/(meanPeakVel*BallPhysics.launchCoefficient*sqrt(BallPhysics.skidEnergyRetention)). Delete DistanceModel or rebase it on BallPhysics so calibration, result panel, and roll agree (today they disagree 2.6x). 2) Make the calibration target real: run (or re-run) the speed calibration in AR against the actually-placed hole distance, or explicitly instruct '8 ft target' in the cal batch, and store the targetDistanceFeet actually used; clamp factor to a plausibility band (~5-40) and reject cal strokes whose peakVel is an outlier (>2 sigma) before averaging. 3) After H4/H5 correct the face-angle definition (rotation about the gravity/world-vertical axis, not ZYX Euler yaw of an inverted device), reinstate per-user bias as a small capped residual: clamp to +/-4 deg, require cal-batch stddev below a threshold, blend across sessions (EMA) instead of trusting one 5-stroke mean, and store pipelineVersion + savedAt in CalibrationProfile with load-time invalidation (bump the ProfileStore key or add a version field; ProfileStore.load already treats decode failure as needs-recalibration). 4) Remove the inconsistent practice-mode display-time baseline subtraction (PracticeSessionView:458-467) or make it use the same corrected definition as AR. 5) Delete CalibrationCoordinator or fix its 0.3 m/s validity gate (rejects every real putt at putting speeds) and its 8 ft default before anyone wires it up. 6) Stalenes
  >> REFUTATION [refute-math]: upheld
     corrections: One direction error, non-load-bearing: the symptom_match parenthetical says the -7.83 deg bias 'would push results LEFT if subtracted'. Wrong in the frame where the bias would actually apply: applyBias = raw - (-7.83 deg) = raw + 7.83 deg, shifting classification toward push/RIGHT and the green-frame sim heading (psi0 = -face) toward -y = right of the target line. It only reads as 'left' on screen if you additionally route through BallRollAnimator's perp = (-aim.z, 0, aim.x) — which is the RIGHT-of-aim vector viewed from above in Y-up ARKit (left-of-aim is (aim.z, 0, -aim.x)), i.e. the green-left -> viewer-right flip belonging to the H4/H5 mapping finding, which this parenthetical never invokes. Either way the core claim (bias applied nowhere, so calibration cannot cause the right-miss) is verified by grep and telemetry. Two minor nits: (a) 'no 0.3 m/s gate' is true, but the practice cal path does have a transitive gate — ImpactDetector snaps strokes < 0.05 m/s to square and !snappedToSquare excludes them (immaterial: 0.05 << 0.3 and all of James's strokes pass); (b) the distance ari
     risk: Unifying the factor on the live BallPhysics chain (~22.8 for James) multiplies every roll ~2.6x; with his actual stroke spread S1/S3 would overshoot a 1 m hole 6-7x. If the on-screen effective scale is already larger than the 14.18 chain predicts (th
     risk: Deleting DistanceModel breaks DistanceModelTests, FuzzTests and SampleRateRobustnessTests and discards the input clamp (maxPlausiblePeakSpeedMps = 5.0) and band/jitter semantics; rebase it on BallPhysics rather than deleting, or port the absurd-peak 
     risk: Reinstating a capped per-user bias BEFORE H4/H5 fix the yaw definition re-bakes the definition error into the profile; ordering is load-bearing (definition fix, then re-learn). Note the bias direction: subtracting today's -7.83 shifts labels toward p
     risk: Bumping the ProfileStore key or adding pipelineVersion silently invalidates existing profiles -> users fall back to defaultSpeedCalibration 14.4 with no UI signal; pair invalidation with an explicit recalibrate prompt (ProfileStore.load already retur
     risk: Outlier rejection (>2 sigma) on a 5-stroke batch is statistically fragile; aggressive rejection can drop the batch below persistCalibrationIfReady's >=3 minimum and brick the cal flow. The factor clamp band (5-40) would NOT have caught this bug (14.1
  >> REFUTATION [fix-regression]: upheld
     corrections: Three refinements, none fatal: (1) "applyBias has zero production callers — only CalibrationTests.swift:198-205 calls it" is slightly incomplete: PressToImpactIntegrationTests.swift:72-73 also exercises the bias arithmetic (inline, not via applyBias) as a deliberate anti-regression canary — this strengthens the finding but is the single biggest landmine for fix step 3, and the fix sketch never mentions that this test must be consciously rewritten. (2) The fix sketch's "Delete DistanceModel or rebase it on BallPhysics" understates the physics conflict: the 2.6x split is launch losses (×0.769) TIMES a genuine friction-model disagreement (~×2.0: 1.666 vs 0.835 m per launch-(m/s)^2). DistanceModel's side is the one anchored to the cited empirical literature (Holmes/Pelz 19.7 ft/s², enforced by SampleRateRobustnessTests.swift:63-69 "tour-pro 1.51 m/s → 10-13 ft"; the BallPhysics chain yields ~6.2 ft for the same launch speed and would FAIL that test). Unifying onto BallPhysics makes the system internally consistent but possibly empirically wrong by ~2x against the project's own cited sour
     risk: Step 1 without step 2: rebasing speedToDistanceFactor on the live BallPhysics chain while persistCalibrationIfReady still hardcodes targetFeet=10.0 gives James factor ~22.8 after recalibration; at his actual ~1m holes every putt rolls ~6x past the cu
     risk: Step 3 (reinstate capped bias) fires the deliberate canary at PuttingLabTests/Physics/PressToImpactIntegrationTests.swift:42-76 (assertion message: 'if this fires, the bias subtraction has been re-enabled somewhere') and contradicts the B78 design ra
     risk: Deleting DistanceModel breaks PuttingLabTests/Physics/DistanceModelTests.swift (entire file), PuttingLabTests/Integration/FuzzTests.swift:41-48, and PuttingLabTests/Integration/SampleRateRobustnessTests.swift:63-73; rebasing it on BallPhysics instead
     risk: Deleting CalibrationCoordinator removes ~9 tests (PuttingLabTests/Calibration/CalibrationTests.swift:7-155, incl. the targetDistanceFeet==8.0 expectation at line 44); alternatively lowering its 0.3 m/s gate silently changes which fixtures are rejecte
     risk: CalibrationProfile schema bump (pipelineVersion/savedAt): the hand-written Codable (CalibrationProfile.swift:31-62) means new non-optional fields invalidate every stored profile — intended (ProfileStore.load returns nil on DecodingError), but it sile
====================================================================================================
KEY: sweep-stance-ux-docs  confirmed=True  conf=high  sev=major
--- explanation:
The sweep confirms a docs/UX split-brain: the authoritative grip (both hands, phone inverted top-toward-floor, long edge at target — docs/putting-stance-reference.md, confirmed by James 2026-06-03) is correctly reflected in the primary in-game copy (PhoneHoldVisual, TestBatch cal instructions), but FOUR places still describe the dead one-hand/vertical hold: PUTT_CALCULATION.md section 3 (worst — written 2026-06-08, AFTER the correction, proving the stale CLAUDE.md section 3 #2 is still seeding regressions), project CLAUDE.md itself, the SensorDebugView onboarding overlay (production-reachable), and CalibrationCoordinator.stalledHint (production-dead but wired-in-tests). Handedness is a stored-but-dead setting: UserProfile carries it through the already-built getUserProfile closure to every stance call site, but StanceGeometry.compute ignores it, BallPhysics hardcodes the RH sign flip (psi0 = -faceAngleRaw, self-labelled HACK), and MarioKartAssist hardcodes RH left/right wording. Height drives only shoulder width; the stance setback value it should also drive (handToBallMetres ~0.295 m) is computed and then never read by anyone — which is exactly why the foot markers sit ON the ball-target line straddling it (spread along perp, zero lateral setback) instead of both on the golfer's side. Finally, the telemetry that future debugging depends on lies: the HUD badge stamps every frame v0.4.8 on a v0.7.14 build, and the placeAddressMarkers log reports a B46 design + PBR translucent material + floor-level stance_y while the code renders opaque UnlitMaterial feet at hardcoded absolute world y=0.08 — concealing the floating-markers bug from log-only analysis.
--- symptom_match:
This sweep does not explain the rolls-missing-right bug (that lives in the sign/marker-geometry sweeps). It DOES fully explain two observed symptoms: (1) 'foot markers fall either side of the ball-target line' — placeAddressMarkers spreads feet along +-perp from stanceCenter=ball with zero lateral setback and no handedness side selection; today's telemetry stance_x/y/z == ball position exactly, foot_half_spread 0.2266 m = 0.245 x 1.85 m / 2, confirming height->spread is the ONLY profile input consumed; (2) why log-driven debugging keeps being misled — the b79 video frames are stamped v0.4.8 and the marker log claims floor-level translucent-PBR B46 markers while reality is opaque yellow planes floating at world y=0.08 (0.933 m above the y=-0.853 floor), matching James's 'markers float in the air'. The doc regression also quantifies process risk: a contradicting hold spec re-entered the repo 5 days after the locked correction, in the doc agents are most likely to read first.
--- fix_sketch:
(1) Copy fixes (one-line each): delete 'keep the phone vertical' from CalibrationCoordinator.stalledHint (suggest 'keep the same grip you pressed with'); rewrite SensorDebugView.swift:461 bullet to 'Hold the phone in your natural putting grip'; rewrite PUTT_CALCULATION.md section 3 to reference putting-stance-reference.md section 2 (both hands, top toward floor, long edge at target) and correct section 6's false handedness claim; amend project CLAUDE.md section 3 decision #2 to point at the stance reference (needs James's OK per stance-ref section 11). (2) Handedness plumbing (corrected design): add stanceSide (or sideSign: +1 RH / -1 LH) to StanceGeometry derived from profile.handedness; in placeAddressMarkers compute stanceLineCenter = ball - perp * Float(stance.handToBallMetres) * sideSign (perp = cross(aim, up) = golfer's right of the line, RH golfer stands on the LEFT side looking ball->hole), spread feet along +-aim by footHalfSpread, rotate foot long axis along perp (toes at the line), and set marker y relative to the floor (ball.y + epsilon), not absolute 0.08; for face-angle sign, thread handedness from userProfile through startRoll -> BallRollAnimator.start -> BallPhysics.simulatePutt and make psi0 = -faceAngleRaw * sideSign (removing the documented HACK), and pass handedness to MarioKartAssist.bucket to flip the 'goes left/right' wording; fork TestBatch B/C left/right-foot wording per handedness. (3) Height: wire the already-computed-but-dead handToBallMetres as the lateral setback (eyes-over-ball geometry); optionally scale foot-marker length (~0.152 x height) —
  >> REFUTATION [refute-math]: upheld
     corrections: The 'FOUR places still describe the dead one-hand/vertical hold' enumeration is incomplete — grep finds at least four MORE stale restatements the sweep missed: README.md:3 ('Hold phone vertically like a putter grip'), GOAL.md:7 ('Phone vertical in lead hand'), goals/in-app-test-session-mode.md:32/35/70 ('Hold the phone vertical, screen TOWARD YOU'), and — most important — .claude/skills/ios-coremotion-arkit-sensors/SKILL.md:12-13, which restates 'vertical in lead hand, screen-facing-user' as a non-negotiable convention and AUTO-LOADS every session per project CLAUDE.md, making it at least as potent a regression-seeder as CLAUDE.md itself. Fixing only the finding's four leaves the seeding mechanism alive. Trivia: SessionCoordinator.appVersionString is lines 80-85 not 80-82, and the file is PuttingLab/SessionCoordinator.swift not UI/; TestBatch cal instructions span 30-39 not 31-38. Framing nuance: the straddling/flanking marker layout was not pure accident — the B77 comment (ARPlacementView 3814-3825) quotes James's own B76 feedback asking for feet 'placed where the ball is', so the p
     risk: Marker height: replacing absolute y=0.08 with ball.y + ~1mm risks resurrecting the B55/B56 invisible-marker bug — the B57 comment (ARPlacementView 3787-3791) says the LiDAR occlusion mesh sits 3-5cm above the real floor, which is why markers were del
     risk: Stance-side setback contradicts the recorded B77 design James himself requested ('placed where the ball is', B76 quote at ARPlacementView 3814-3825) — get explicit sign-off citing that quote before moving feet to one side, or he may report the marker
     risk: psi0 = -faceAngleRaw * sideSign for left-handers is plausible but unverified: a LH grip mirrors WHICH long edge faces the target, and the yaw-delta→open/closed sign mapping must be confirmed with at least one instrumented LH stroke before shipping; s
     risk: placeAddressMarkers has a defaulted stance parameter (= .compute(profile: .default)); adding handedness/side to the signature with another default invites silent RH fallback at any future call site — prefer no default, and derive the log design strin
     risk: The copy sweep must include the four additional stale locations (README.md:3, GOAL.md:7, goals/in-app-test-session-mode.md, and the AUTO-LOADED .claude/skills/ios-coremotion-arkit-sensors/SKILL.md:12-13) or fresh agent sessions keep re-ingesting the 
  >> REFUTATION [fix-regression]: upheld
     corrections: Three things understated/wrong in the finding+fix, none fatal: (1) Call-site undercount — placeAddressMarkers has 7 call sites (780, 1907, 1930, 1947, 4810, 4830, 4844 in ARPlacementView.swift), not "three tap call sites plus Settings onSave"; lines 1907/1930/1947 are a separate View-layer crosshair/move path the sweep missed. All are profile-fed so the conclusion (handedness already arrives everywhere) still holds. (2) Fix-sketch flaw: "set marker y relative to the floor (ball.y + epsilon)" contradicts the in-code B55→B57 lesson at ARPlacementView.swift:3787-3794 — 1 cm markers were invisible under the LiDAR mesh occlusion plane (sits 3-5 cm above the real floor); the offset must be ~ball.y + 0.05, not +1 mm, or the floating-feet fix trades into an invisible-feet regression on LiDAR iPhones. (3) Missed coupling: SettingsView.swift:67 footer ("v1 foot markers are symmetric and face-angle sign is right-hand-mirrored") becomes false the moment marker side-selection consumes handedness — it must be rewritten in the same change. Minor: SensorDebugView is concretely presented via Practice
     risk: LiDAR occlusion regression: 'ball.y + epsilon' re-creates the B55/B56 invisible-marker bug (LiDAR mesh occlusion plane sits 3-5cm above the floor per ARPlacementView.swift:3787-3794); use ball.y + ~0.05 m or exclude markers from scene-mesh occlusion.
     risk: Visibility/frustum regression: B77 deliberately moved markers to flank the ball because stance-side geometry was invisible at address (pre-B77 60cm-behind markers were frustum-culled; B70 added a 2D HUD workaround, removed in B77 — ARPlacementView.sw
     risk: Rotation sign hazard: the new layout spreads feet along ±aim with the long axis along perp — a 90° yaw change interacting with B79's generatePlane width/depth swap (ARPlacementView.swift:3797-3805); this exact bug class shipped twice ('yellow tally m
     risk: Atomicity of handedness: if marker side-selection, BallPhysics psi0 sign (psi0 = -faceAngleRaw*sideSign), MarioKartAssist wording flip, and the SettingsView footer rewrite do not land together, an LH user flipping the Settings toggle gets self-contra
     risk: Signature changes must use defaulted parameters (handedness: .right): non-defaulted params break compile in MarioKartAssistTests/BallPhysicsTests/FuzzTests, PuttHistoryView.swift:18-21, and ARPlacementView.swift:281; also keep tools/simulate_historic
====================================================================================================
DESIGNS: ['Stance v2 — One-Sided Golf Address Markers (handedness-aware, floor-relative, line-facing feet)', 'PuttingLab Render-Frame Repair v3 — single left-handed-perp + golf-sign convention, atomic producer+renderer fix']
